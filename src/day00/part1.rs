use crate::day00::input::Input;

pub fn solve(input: &Input) -> Option<usize> {
    None
}

#[cfg(test)]
mod test {
    use super::*;

    use crate::day00::input::read;

    #[test]
    fn test_solve() {
        assert!(true)
    }
}
